<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Fullscreen">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="TextInputLayoutTheme" parent="TextAppearance.AppCompat">
        <item name="android:textColorPrimary">@color/white</item>
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/textColorPrimary</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/textColorPrimary</item>
        <item name="colorControlNormal">@color/textColorPrimary</item>
        <item name="colorControlActivated">@color/textColorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Divider">
        <item name="android:background">?android:attr/listDivider</item>
    </style>

    <style name="Divider.Horizontal" parent="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
    </style>

    <style name="Plain" parent="BottomDialogTheme">
        <item name="bottom_sheet_extra_padding">0dp</item>
        <item name="should_draw_under_status_bar">false</item>
    </style>

    <style name="Pull" parent="BottomDialogTheme">
        <item name="bottom_sheet_extra_padding">0dp</item>
        <item name="bottom_sheet_corner_radius">26dp</item>
        <item name="should_draw_content_under_handle_view">true</item>
    </style>

    <style name="Rotate" parent="BottomDialogTheme">
        <item name="bottom_sheet_extra_padding">0dp</item>
        <item name="bottom_sheet_corner_radius">26dp</item>
        <item name="should_draw_content_under_handle_view">true</item>
    </style>

</resources>
